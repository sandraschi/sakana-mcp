FROM python:3.12-slim

WORKDIR /workspace

ENV PYTHONUNBUFFERED=1

COPY pyproject.toml /workspace/pyproject.toml
COPY src /workspace/src

RUN python -m pip install --upgrade pip && pip install -e .

CMD ["python", "-m", "sakana_mcp.server"]

